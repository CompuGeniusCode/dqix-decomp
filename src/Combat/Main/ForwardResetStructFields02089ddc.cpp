#include <globaldefs.h>

struct ResetFieldsStruct;
void ResetStructFields(struct ResetFieldsStruct* s);

// USA: func_02089ddc
ARM void ForwardResetStructFields02089ddc(struct ResetFieldsStruct* s) {
    ResetStructFields(s);
}
