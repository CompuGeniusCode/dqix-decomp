#include <globaldefs.h>

// AUTO-SCAFFOLD: names/addresses exact, facts derived from the binary, listing annotated.
// Do NOT re-grep symbols or the disassembly — it is all below. Callee SIGNATURES are the
// one guess; fix them as you go and delete declarations you do not call.


// USA: func_02000aac
extern "C" ARM int AutoloadCallback(/* TODO args */) {
    /* TODO */
}
